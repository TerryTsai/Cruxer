server:
  port: 80

multipart:
  maxFileSize: 3Mb
  maxRequestSize: 3Mb

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cruxer
    username: cruxer
    password: cruxer

  #redis:
  #  host: localhost
  #  port: 6379

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create-drop
      #ddl-auto: create
      #ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57InnoDBDialect

  thymeleaf:
    mode: LEGACYHTML5
    cache: false
    enabled: true
    suffix: .html
    prefix: file:site/

  mail:
      host: smtp.gmail.com
      port: 587
      personal: Cruxer
      username: #####
      password: #####
      protocol: smtp
      tls: true
      properties.mail.smtp:
          auth: true
          starttls.enable: true
          ssl.trust: smtp.gmail.com

logging.level.org.springframework: INFO

upload.file:
  url: http://localhost/files/
  path: files/
  charset: ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789
  length: 25
